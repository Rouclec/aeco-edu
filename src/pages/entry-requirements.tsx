import { TopNav } from "@/components";
import React, { FC } from "react";

const Main: FC = () => {
  return (
    <TopNav>
      <div>Entry Requirements</div>
    </TopNav>
  );
};

export default Main;
