import { TopNav } from "@/components";
import React, { FC } from "react";

const Main: FC = () => {
  return (
    <TopNav>
      <div>Partner with us</div>
    </TopNav>
  );
};

export default Main;
