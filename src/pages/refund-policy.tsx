import { TopNav } from "@/components";
import React, { FC } from "react";

const Main: FC = () => {
  return (
    <TopNav>
      <div>Refund policies</div>
    </TopNav>
  );
};

export default Main;
