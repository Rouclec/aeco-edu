import { TopNav } from "@/components";
import React, { FC } from "react";

const Main: FC = () => {
  return (
    <TopNav>
      <div>Student protection</div>
    </TopNav>
  );
};

export default Main;
