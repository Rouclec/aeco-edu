import { TopNav } from "@/components";
import React, { FC } from "react";

const Main: FC = () => {
  return (
    <TopNav>
      <div>Local student placements</div>
    </TopNav>
  );
};

export default Main;
